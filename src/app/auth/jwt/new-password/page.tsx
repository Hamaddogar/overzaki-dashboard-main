// sections
import { JwtNewPasswordView } from 'src/sections/auth/jwt';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Auth: New Password',
};

export default function ModernNewPasswordPage() {
  return <JwtNewPasswordView />;
}
