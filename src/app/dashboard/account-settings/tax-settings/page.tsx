// sections

import { TaxSettingsView } from "src/sections/account-settings/view";

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Dashboard: Tax Settings',
};

export default function TaxSettingsViewPage() {
  return <TaxSettingsView />;
}
