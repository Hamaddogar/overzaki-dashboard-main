// sections

import { DeliveryPickupNewLocation } from "src/sections/Delivery-Pickup/view";

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Dashboard: New Location',
};

export default function AddNewLocationPage() {
  return <DeliveryPickupNewLocation />;
}
